models:
  - type: main
    engine: google_genai
    model: gemini-3.8-flash
    api_key_env_var: GEMINI_API_KEY
    parameters:
      temperature: 0.0
      max_retries: 0

rails:
  input:
    flows:
      - self check input